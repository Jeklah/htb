use the following commands to use redis-cli

keys * - get index of databases
select <number> - select database
get <name of database, will say next to index from keys *> - get value


    for "string": get <key>
    for "hash": hgetall <key>
    for "list": lrange <key> 0 -1
    for "set": smembers <key>
    for "zset": zrange <key> 0 -1 withscores

